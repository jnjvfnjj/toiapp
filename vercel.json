{
  "buildCommand": "cd frontend && npm ci && npm run build && cd .. && cd backend && python manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "backend/config/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "^/(.*)$",
      "dest": "backend/config/wsgi.py"
    }
  ]
}

